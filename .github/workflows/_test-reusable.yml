name: Test Reusable
on:
  workflow_call:
    inputs:
      app_name:
        required: true
        type: string

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from reusable workflow, app=${{ inputs.app_name }}"
