# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║  Docu-Update Agent — Stufe 1+2                                            ║
# ║  Trigger: Issue mit Label "docu-update" oder manuell via workflow_dispatch ║
# ║  Stufe 1: deterministisch (README version + CHANGELOG aus git log)        ║
# ║  Stufe 2: --llm (litellm → gpt-4o-mini für CHANGELOG-Zusammenfassungen)  ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

name: Docu-Update Agent

on:
  issues:
    types: [labeled]

  workflow_dispatch:
    inputs:
      issue_number:
        description: 'Issue-Nummer manuell starten (z.B. 46)'
        required: true
        type: number
      dry_run:
        description: 'Dry-run (kein Push, kein Issue-Close)'
        required: false
        type: boolean
        default: false
      use_llm:
        description: 'Stufe 2: LLM-enhanced (gpt-4o-mini)'
        required: false
        type: boolean
        default: true

permissions:
  issues: write
  contents: read

jobs:
  docu-update:
    name: "Docu-Update: Issue #${{ github.event.issue.number || inputs.issue_number }}"
    runs-on: ubuntu-latest
    timeout-minutes: 10

    # Nur bei docu-update Label (oder manuellem Trigger)
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'issues' &&
       github.event.action == 'labeled' &&
       github.event.label.name == 'docu-update')

    steps:
      - name: Checkout platform repo
        uses: actions/checkout@v7

      - name: Set up Python 3.12
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      # Installiert wird, was das Skript IMPORTIERT: httpx + litellm.
      # Vorher stand hier `aifw` — nur als Traeger fuer die transitive
      # litellm-Abhaengigkeit. Zwei Fehler in einem: (1) die Distribution
      # heisst seit dem Umzug `iil-aifw`; `aifw` ist auf PyPI vollstaendig
      # zurueckgezogen (alle 5 Releases yanked, letzte 0.5.0) -> pip meldet
      # "no matching distribution found for aifw" und der Lauf faellt
      # (2026-08-09, Issue #1850); (2) `docu_update_agent.py` importiert
      # `litellm` ohnehin direkt (Regel in seinem Docstring: aifw in
      # Django-Apps, litellm in Standalone-Skripten) — aifw war nie noetig.
      - name: Install dependencies
        run: pip install httpx litellm

      - name: Run Docu-Update Agent
        env:
          PROJECT_PAT: ${{ secrets.PLATFORM_DEPLOY_TOKEN || secrets.PROJECT_PAT }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}  # litellm reads this
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          ISSUE_NUM="${{ github.event.issue.number || inputs.issue_number }}"
          DRY_RUN="${{ inputs.dry_run == true && '--dry-run' || '' }}"

          # Stufe 2 default bei workflow_dispatch, Stufe 1 bei auto-trigger
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            LLM_FLAG="${{ inputs.use_llm == true && '--llm' || '' }}"
          else
            LLM_FLAG="--llm"
          fi

          python .github/scripts/docu_update_agent.py \
            --issue-number "$ISSUE_NUM" \
            $DRY_RUN $LLM_FLAG
