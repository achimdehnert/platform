# AGENTS.md — platform

Einstieg für jeden Coding-Agenten: **[`CORE_CONTEXT.md`](CORE_CONTEXT.md)** (Rolle, Konventionen, Tests, Guard), dann `AGENT_HANDOVER.md` (aktueller Stand). Claude Code lädt zusätzlich `CLAUDE.md`. Tests nur über `make test`; editieren nur im Session-Worktree (`bash tools/repo-session.sh start ~/github/platform --task <slug>`).
