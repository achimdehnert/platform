[flake8]
max-line-length = 100
extend-ignore =
    # Django related
    E203,  # whitespace before ':' (conflicts with black)
    E501,  # line too long (handled by black)
    W503,  # line break before binary operator (conflicts with black)
    # F-strings without placeholders are fine
    F541,
    # Allow lambdas (useful in Django)
    E731,

exclude =
    .git,
    __pycache__,
    .venv,
    venv,
    .tox,
    dist,
    build,
    *.egg,
    migrations,
    staticfiles,
    media,
    node_modules,
    .pytest_cache,
    htmlcov,

per-file-ignores =
    __init__.py: F401
    settings/*.py: F401,F403,F405
    tests/*.py: F401,F811

# Django specific
django-settings-module = config.settings

# Max complexity
max-complexity = 10
