# ═══════════════════════════════════════════════════════════════════════════════
# authentik — Environment Variables (ADR-142)
# ═══════════════════════════════════════════════════════════════════════════════
# Copy to .env and fill in values. NEVER commit .env to Git.
# Generate secrets: python3 -c "import secrets; print(secrets.token_hex(32))"
# ═══════════════════════════════════════════════════════════════════════════════

# PostgreSQL
AUTHENTIK_DB_USER=authentik
AUTHENTIK_DB_PASS=CHANGE_ME_STRONG_PASSWORD

# Redis
AUTHENTIK_REDIS_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# authentik
AUTHENTIK_SECRET_KEY=CHANGE_ME_STRONG_SECRET_KEY
AUTHENTIK_HOST=https://id.iil.pet
