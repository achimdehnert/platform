# Restore-Feuerübung 2026-09-24 — risk_hub_db (risk_hub)

> ADR-241 §5 / #2284 K4 · erzeugt von `infra/host-maintenance/restore-drill.sh` via `make restore-drill`
> Ausführung: Kapitäns-Kanal (Claude Code), Host `prod`, ohne Rückstände (Wegwerf-Container + Verzeichnis per trap entfernt)

| Feld | Wert |
|---|---|
| Quell-Snapshot | `cc8b61e6` · Tag `risk_hub_db` · 2026-09-24T12:37:30Z (9 h alt) |
| Dump | `risk_hub_db.sql` (pg_dumpall), 3 MB |
| Restore-Ziel | Wegwerf-Container `restore-drill-20260924T222924Z`, Image `postgres:16-alpine`, Datenverzeichnis unter `/mnt/HC_Volume_105908261/restore-drill` |
| Smoke-Query | `select count(*) from dsb_technical_measure` |
| Ergebnis restauriert | **352** |
| Ergebnis Prod (live) | **352** |
| Tabellen in `risk_hub` nach Restore | 202 |
| psql-Fehler | 2 gesamt, davon 0 unerwartet (erwartet: Rolle/DB `already exists`) |
| RTO-Ist | **30 s** gesamt, davon 22 s Einspielen · Soll 4 h |
| Urteil | ✅ bestanden |

## Wiederholen

```
make restore-drill                       # Vorgabe: risk_hub_db / risk_hub / dsb_technical_measure
make restore-drill APP=<container> DB=<db> TABLE=<tabelle>
```

Quartalsweise (ADR-241). `backup_meter.py --enforce-drill` meldet ab 100 Tagen ohne neues Protokoll eine Verletzung.
