[Unit]
Description=Daily GC of merged git session-worktrees (prevent worktree-orphan accumulation)

[Timer]
OnCalendar=*-*-* 05:00:00
Persistent=true
RandomizedDelaySec=900

[Install]
WantedBy=timers.target
