# Dev-/Test-Dependencies für platform (Meta-Repo — keine Runtime-App).
# Install: make setup   (oder: pip install -r requirements-dev.txt)
# CI-Referenz: .github/workflows/tools-tests.yml installiert pytest+pyyaml;
# diese Datei ist die lokale SSoT dafür (repo-optimize X-3, 2026-07-02).
#
# Pin-Style (==) statt >=-Range seit CI-4 (Issue #1202): dependabot/fetch-metadata@v2
# kann bei >=-Range-Constraints previous-version/new-version nicht auflösen
# (update-type bleibt null → dependabot-automerge.yml greift nie). Bei exakten
# Pins liefert der Dependabot-PR-Diff konkrete Alt-/Neu-Version, wodurch die
# semver-Klassifizierung (patch/minor/major) wieder funktioniert.
pytest==9.1.1
pyyaml==6.0.3
ruff==0.16.8
# Rotationswerkzeug (platform#2813): jsonschema prueft infra/secrets-inventory.yaml
# gegen infra/schemas/, pynacl versiegelt Werte fuer die GitHub-Secrets-API.
jsonschema==4.26.0
pynacl==1.6.2
pre-commit==4.6.2
# ADR-Schema-Hook (.pre-commit-config.yaml) braucht die adrfw-CLI. Bewusst
# ungepinnt gelassen (kein >=-Range-Constraint vorher, anderer Fall als CI-4) —
# seit platform#3457 Schritt 3 mit Versions-Untergrenze: tools/adr/adr_analyze.py
# importiert iil_adrfw.rules.drift, das Modul existiert erst ab 0.9.0.
iil-adrfw>=0.9.0,<1
httpx==0.28.1
