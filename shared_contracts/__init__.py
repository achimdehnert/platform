# shared_contracts — ADR-057 §2.8/§2.9
# Cross-service contract schemas for Celery task payloads and DB views.
# Install in consuming repos: pip install -e path/to/platform/shared_contracts
# Or copy task_schemas.py / db_views.py directly into the consuming repo's tests/contracts/
