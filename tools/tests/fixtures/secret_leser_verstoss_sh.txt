#!/usr/bin/env bash
# Positivkontrolle fuer test_secret_lesen.py — ABSICHTLICH falsch.
# Wird als .sh nach tmp_path kopiert und muss vom Melder gefunden werden.
set -euo pipefail
GEHEIM="$HOME/.secrets/beispiel_token"
TOKEN="$(cat "$GEHEIM")"
echo "${#TOKEN}"
